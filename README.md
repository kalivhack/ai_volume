# ai_volume
Programm in python what can low and up volume with hands tracking  

commands for currect working the script:

pip install numpy && pip install opencv-python && pip install comtypes && pip install ctypes && pip install mediapipe && pip install pycaw
